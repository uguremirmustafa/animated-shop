import React from 'react';
import Layout from '../layouts/Layout';

interface Props {}

const Cart = ({}: Props) => {
  return (
    <Layout>
      <div className="cart">
        <h2>cart</h2>
      </div>
    </Layout>
  );
};

export default Cart;
